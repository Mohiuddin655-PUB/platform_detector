# platform_detector_example

A simple Flutter app demonstrating the usage of the `platform_detector` package.

## Run

```bash
cd example
flutter pub get
flutter run
```

## What it shows

- Current platform name (`platform`)
- All boolean getters with live values
- Real-time check on whichever device/browser you run it on